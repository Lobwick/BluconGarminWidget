# This is a basic workflow to help you get started with Actions

name: CI 

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - run: java -cp java HelloWorldApp -Dfile.encoding=UTF-8 -Dapple.awt.UIElement=true -jar /Users/Felix/Library/Application Support/Garmin/ConnectIQ/Sdks/connectiq-sdk-mac-3.2.5-2021-02-12-6d31f4357/bin/monkeybrains.jar -o /Users/Felix/garmin/BluconGarminWidget/bin/BluconGarminWidget.prg -w -y /Users/Felix/garmin/eclipse_garmin/developer_key -f /Users/Felix/garmin/BluconGarminWidget/monkey.jungle 


    
